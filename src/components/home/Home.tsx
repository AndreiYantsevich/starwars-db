import React, {FC} from 'react';
import styles from './MainPage.module.css';

const Home: FC = () => {
    return (
        <div className={styles.container}>

        </div>
    );
};

export default Home;